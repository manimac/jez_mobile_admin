import { LoadServicesPipe } from './load-services.pipe';

describe('LoadServicesPipe', () => {
  it('create an instance', () => {
    const pipe = new LoadServicesPipe();
    expect(pipe).toBeTruthy();
  });
});
